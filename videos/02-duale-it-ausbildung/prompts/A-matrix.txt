2x2 matrix diagram in German for a clean infographic, where the top-right quadrant contains TWO stacked sub-boxes instead of one.

Top center headline in bold mixed case: "Die vier dualen IT-Berufe"

Below the headline, a 2x2 grid of four quadrant cells, but the top-right cell is split horizontally into TWO sub-boxes (top half and bottom half). Result: three single boxes (top-left, bottom-left, bottom-right) and one split cell (top-right) containing two sub-boxes. All boxes have rounded corners (5 pixel radius). Generous white gap between cells (about 40 pixels), smaller gap (about 10 pixels) between the two stacked sub-boxes in the top-right cell.

Around the 2x2 grid, two axis labels in regular dark gray text:
- Above the grid, centered horizontally: "technisch"
- Below the grid, centered horizontally: "kaufmännisch"
- To the left of the grid, vertically rotated 90 degrees counter-clockwise (reading bottom to top): "Hardware-nah"
- To the right of the grid, vertically rotated 90 degrees clockwise (reading top to bottom): "Software- und Prozess-nah"

All boxes have the same dark blue fill (#1E3A8A) and white text. Each box contains exactly two text lines centered:
- Top line in bold white text: the profession name
- Bottom line in regular white text in slightly smaller size: one single German verb in single quotes

Top-left box (full quadrant):
- Top line: "IT-System-Elektroniker"
- Bottom line: "'aufbauen'"

Top-right cell, upper sub-box:
- Top line: "Fachinformatiker Anwendungsentwicklung"
- Bottom line: "'programmieren'"

Top-right cell, lower sub-box:
- Top line: "Fachinformatiker Systemintegration"
- Bottom line: "'Netzwerke'"

Bottom-left box (full quadrant):
- Top line: "Kaufmann IT-System-Management"
- Bottom line: "'beraten'"

Bottom-right box (full quadrant):
- Top line: "Kaufmann Digitalisierungsmanagement"
- Bottom line: "'digitalisieren'"

Absolutely NO other text inside the boxes. NO sub-descriptions, NO bullet points, NO icons, NO arrows between boxes.

Render as a clean technical infographic in the style of a scientific paper or
textbook diagram (like draw.io, Lucidchart, IEEE figures):
- White background (#FFFFFF)
- Crisp geometric shapes only: rectangles with rounded corners (4 to 6 pixel radius)
- Solid flat fills, NO gradients, NO glow, NO neon, NO drop shadows, NO blur
- Color palette: dark blue (#1E3A8A) primary with white text, dark gray (#1F2937) for axis labels and headline
- Sans-serif typography only (Inter / Helvetica / Arial), bold for headline and profession names, regular for axis labels and the quoted verbs
- Each box contains ONLY the profession name and the single quoted verb, absolutely NO sub-text, NO descriptive sub-lines
- Generous white space, perfectly grid-aligned, symmetric composition
- Render German umlauts correctly: ä, ö, ü, ß. Do not transliterate to ae, oe, ue, ss.
- Mobile-readable: every label large enough at thumbnail size
- NO hand-drawn or sketch aesthetic, NO whiteboard, NO 3D, NO illustrations of robots or characters, NO photorealistic elements
- Aesthetic: looks like a figure from a computer science textbook or a Notion/Linear product diagram
